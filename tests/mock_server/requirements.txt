fastapi==0.136.1
uvicorn[standard]==0.47.0
# Latest v3-compatible majors so the mock server speaks Engine.IO v3,
# matching the vendored Abode SocketIO client (and real Abode). See #105.
# Pinned exactly to keep the Docker build reproducible.
python-socketio==4.6.1
python-engineio==3.14.2
